package com.example.uiarchitecture.feature.addpole

import androidx.compose.runtime.Composable
import androidx.compose.ui.tooling.preview.Preview

@Composable
fun AddPoleScreen(
    state: AddPoleState = AddPoleState(),
    actions: AddPoleActions = AddPoleActions()
) {
    // TODO UI Logic
}

@Composable
@Preview(name = "AddPole")
private fun AddPoleScreenPreview() {
    AddPoleScreen()
}

